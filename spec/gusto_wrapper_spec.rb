require 'spec_helper'

describe GustoWrapper do
  it 'has a version number' do
    expect(GustoWrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
